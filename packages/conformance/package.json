{
  "name": "@tinfoilsh/conformance",
  "version": "0.1.0",
  "private": true,
  "description": "Tinfoil cross-SDK conformance binary (JS implementation)",
  "type": "module",
  "bin": {
    "tinfoil-conformance": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tinfoilsh/verifier": "1.1.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
